// create an array variable containing the names
dogs = ["Chena", "Abby", "Kiya", "Chula", "Sitka", "Ohana"]

// write a for loop that prints each name on a different line
for (let i = 0; i < dogs.length; i++) {
  console.log(dogs[i])
}